<%- pages = nav_pages(current_resource.url) -%>

<aside class="specs-side-navigation">
  <ul>
    <% (sub_pages("features/technicalspecifications").sort_by { |c| c.data.rang }).each do |resource| %>
      <% if current_resource.data.title == resource.data.title %>
        <li>
          <div class="active item">
            <a href="#"><span><%= resource.data.sname %></span></a>
          </div>
        </li>
      <% else %>
        <li class="item">
          <%= link_to resource.data.sname, resource.url %>
        </li>
      <% end %>
    <% end %>
  </ul>
</aside>
